name: dixo_sematext_logger
description: A Flutter package that provides Sematext logging implementation.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=3.13.9"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  hive: ^2.2.3
  collection: ^1.17.2
  shared_preferences: ^2.2.3
  
  logger:
    path: '../logger'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.9

flutter: